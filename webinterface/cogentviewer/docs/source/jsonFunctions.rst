================
JSON Interface
================


Json Nav
=========

This method returns the database in a list structure suitable 
for display in a Dojo Tree Structure.
It Relies on the :func:`model.asList` function of the model classes

Each Element of the Tree will have the following attributes (defined by the :func:`model.asJSON`)



.. graphviz::
   
   asList.dot
   	   

